/mon_application_flask
│
├── app.py                  # Le script principal Flask qui définit les routes de l'API
│
├── /templates              # Dossier contenant les fichiers HTML pour l'interface utilisateur
│   └── index.html          # La page HTML où l'utilisateur entre les données pour la prédiction
│
└── /static                 # Dossier contenant les fichiers statiques comme CSS et JavaScript
    └── styles.css          # Fichier CSS pour styliser la page web index.html
